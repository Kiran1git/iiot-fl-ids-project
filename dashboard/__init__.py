"""Read-only Streamlit dashboard package for the IIoT Federated IDS project.

Package marker only. The dashboard entry point is ``dashboard/app.py``, launched
with ``streamlit run dashboard/app.py``. No dashboard module ever trains,
partitions, preprocesses, or writes to ``outputs/`` or ``data/``.
"""
